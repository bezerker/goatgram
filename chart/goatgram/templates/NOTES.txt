Goatgram deployed!

  Release: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

{{- if .Values.ingress.enabled }}
The Ingress resource is templated by this chart and configured for
{{ .Values.ingress.className }}.

Hosts:
{{- range .Values.ingress.hosts }}
  - {{ .host }}
{{- end }}

Ensure DNS records point to your ingress controller's external address.
{{- else }}
An Ingress is not enabled. To expose the application, either:
  - Set ingress.enabled=true and configure ingress hosts, or
  - Create a Cloudflare Tunnel manually with cloudflared pointing to
    http://{{ include "goatgram.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
{{- end }}

{{- if .Values.hpa.enabled }}
HPA is enabled (min={{ .Values.hpa.minReplicas }}, max={{ .Values.hpa.maxReplicas }}).
{{- end }}
